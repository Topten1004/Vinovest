import React from "react";
import PriceHistory from "./components/PriceHistory";

const ChartScreen = () => {
    return (
        <div>
            <PriceHistory />
        </div>
    );
};

export default ChartScreen;
