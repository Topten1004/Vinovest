import styled from "styled-components";

const StyledBadge = styled.div`
    background: #70808c;
    color: #ffffff;
    padding: 3px 5px;
    text-align: center;
    font-weight: 500;
`;

export default StyledBadge;
